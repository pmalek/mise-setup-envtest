> NOTE: This plugin is not needed anymore because `mise` supports the [`github` backend][mise_gh_backend] natively.
>
> This means that users can simply run:
> ```
> mise install github:kubernetes-sigs/controller-runtime
> ```

[mise_gh_backend]: https://mise.jdx.dev/dev-tools/backends/github.html

# mise-setup-envtest

[`setup-envtest`](https://github.com/kubernetes-sigs/controller-runtime/tree/main/tools/setup-envtest) plugin for the [mise](https://github.com/jdx/mise) version manager.

## Install

```
mise plugin install setup-envtest https://github.com/pmalek/mise-setup-envtest.git
```

## Use

Check out the [mise](https://github.com/jdx/mise) readme for instructions on how to install and manage versions of `gotestsum`.
